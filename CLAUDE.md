# CLAUDE.md

Read [AGENTS.md](AGENTS.md) — single source of truth: hard rules (fail-closed
resolver, vanilla Rails first), git workflow (PRs always), the pre-commit
review gate (ce → ie → cubic), tool playbook (Augment-first discovery), testing
policy (Minitest + cuprite system tests, stable DOM ids).
