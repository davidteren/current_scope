<% content_for :title, "New claim" %>

<h1>New claim</h1>
<p class="mono" style="color: var(--muted); margin-top: -0.75rem;">
  submitted by <%= Current.user.email_address %> — the four-eyes rule means you
  won't be able to approve it yourself
</p>

<%= render "form", expense_claim: @record %>

<div class="actions-row">
  <%= link_to "Back to expenses", expense_claims_path %>
</div>
