#!/usr/bin/env ruby
# This command will automatically be run when you run "rails" with Rails gems
# installed from the root of your application.

ENGINE_ROOT = File.expand_path("..", __dir__)
ENGINE_PATH = File.expand_path("../lib/current_scope/engine", __dir__)
APP_PATH = File.expand_path("../test/dummy/config/application", __dir__)

# Set up gems listed in the Gemfile.
ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../Gemfile", __dir__)
require "bundler/setup" if File.exist?(ENV["BUNDLE_GEMFILE"])

# Coverage must start before the `require "rails/engine/commands"` below, which is
# what ends up loading the engine — Coverage cannot instrument a file retroactively.
# `t` is railties' alias for `test` (rails/commands.rb) and resolves further down the
# stack than this line, so it has to be matched here. Why this matters, and what
# breaks if it moves: test/coverage_setup.rb.
require_relative "../test/coverage_setup" if ARGV.first&.match?(/\A(t|test)(:|\z)/)

require "rails/all"
require "rails/engine/commands"
